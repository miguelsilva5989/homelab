apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: xupa-ingressroute-certificate
  namespace: xupa
spec:
  secretName: xupa-certificate-secret
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - xupa.milanchis.com
