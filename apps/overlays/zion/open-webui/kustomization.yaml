apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: open-webui

resources:
  - ../../../base/open-webui
  - configmap.yaml
  - ingress.yaml

patches:
  - path: patch-replicas.yaml
    target:
      kind: Deployment
