apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: media

namePrefix:
  zion-

metadata:  
  name: jellyfin
  namespace: media

resources:
  - ../../../base/jellyfin
  - certificate.yaml
  - ingress.yaml
  - middleware.yaml
