apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: umami-ingressroute-certificate
  namespace: umami
spec:
  secretName: umami-certificate-secret
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - analytics.streamsquire.app
