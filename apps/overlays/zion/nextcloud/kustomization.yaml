apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: media

namePrefix:
  zion-

metadata:  
  name: nextcloud
  namespace: media

resources:
  - ../../../base/nextcloud
  - certificate.yaml
  - ingress.yaml
  - middleware.yaml
