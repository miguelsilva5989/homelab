apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: glyphforge-ingressroute-certificate
  namespace: glyphforge
spec:
  secretName: glyphforge-certificate-secret
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - glyphforge.milanchis.com
