apiVersion: v1
kind: Service
metadata:
  name: streamsquire-web
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  selector:
    app: streamsquire-web
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
