apiVersion: v1
kind: Service
metadata:
  name: registry-ui
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  selector:
    app: registry-ui
  ports:
  - name: registry-ui
    protocol: TCP
    port: 80
    targetPort: 80
