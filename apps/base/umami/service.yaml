apiVersion: v1
kind: Service
metadata:
  name: umami
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  selector:
    app: umami
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
