apiVersion: v1
kind: Service
metadata:
  name: flatproof
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  selector:
    app: flatproof
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
