apiVersion: v1
kind: Service
metadata:
  name: glyphforge
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  selector:
    app: glyphforge
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
